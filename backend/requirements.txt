fastapi
uvicorn[standard]
pydantic
pydantic-settings
httpx

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
alembic>=1.12.0  # Database migrations
APScheduler>=3.10.1  # Scheduler for background token refresh jobs

# RAG Pipeline - Embeddings & Vector Store
sentence-transformers>=2.2.0
chromadb>=0.4.0
openai>=1.0.0

# RAG Pipeline - Retrieval & Generation
litellm>=1.0.0  # Unified LLM API wrapper (supports OpenAI, Ollama, HuggingFace, etc.)
rank-bm25>=0.2.2  # BM25 keyword search for hybrid search

# RAG Pipeline - Document Loaders
pdfplumber>=0.10.0  # PDF text extraction with structure preservation
python-docx>=1.1.0  # DOCX file parsing and text extraction

# Background Processing
rq==1.15.2  # Redis Queue for async job processing

# Advanced File Features
pytesseract==0.3.10  # OCR for scanned PDFs
trafilatura==1.6.3   # Web content extraction
python-magic==0.4.27 # File type detection

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT token handling
bcrypt>=4.0.0  # Password hashing
python-multipart>=0.0.6  # For form data parsing
redis>=4.5.0  # Redis for rate limiting and session management
pyotp>=2.9.0  # TOTP for Two-Factor Authentication
qrcode[pil]>=7.4.0  # QR code generation for 2FA setup

# Optional: encryption support for OAuth tokens
cryptography>=40.0.0

# Email
resend>=0.1.0  # Resend email API

# PDF generation (used for export jobs)
reportlab>=3.6.12

# Billing
stripe>=8.0.0  # Stripe Python SDK for billing and Checkout

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # For FastAPI TestClient
